@PACKAGE_INIT@

SET(CERNLIB_VERSION @CERNLIB_VERSION@)
SET(CERNLIB_VERSION_MAJOR  @CERNLIB_VERSION_MAJOR@)
SET(CERNLIB_VERSION_MINOR  @CERNLIB_VERSION_MINOR@)
SET(CERNLIB_VERSION_PATCH  @CERNLIB_VERSION_PATCH@)

set_and_check(CERNLIB_INCLUDE_DIR @PACKAGE_CMAKE_INSTALL_INCLUDEDIR@)

find_library(CERNLIB_PAWLIB         NAMES pawlib          HINTS @PACKAGE_CMAKE_INSTALL_LIBDIR@ NO_DEFAULT_PATH)
find_library(CERNLIB_PAWLIBLESSTIF  NAMES pawlib-lesstif  HINTS @PACKAGE_CMAKE_INSTALL_LIBDIR@ NO_DEFAULT_PATH)
find_library(CERNLIB_KERNLIB        NAMES kernlib         HINTS @PACKAGE_CMAKE_INSTALL_LIBDIR@ NO_DEFAULT_PATH)
find_library(CERNLIB_PACKLIB        NAMES packlib         HINTS @PACKAGE_CMAKE_INSTALL_LIBDIR@ NO_DEFAULT_PATH)
find_library(CERNLIB_PACKLIBLESSTIF NAMES packlib-lesstif HINTS @PACKAGE_CMAKE_INSTALL_LIBDIR@ NO_DEFAULT_PATH)
find_library(CERNLIB_GRAFLIB        NAMES graflib         HINTS @PACKAGE_CMAKE_INSTALL_LIBDIR@ NO_DEFAULT_PATH)
find_library(CERNLIB_GRAFX11        NAMES grafX11         HINTS @PACKAGE_CMAKE_INSTALL_LIBDIR@ NO_DEFAULT_PATH)
find_library(CERNLIB_MATHLIB        NAMES mathlib         HINTS @PACKAGE_CMAKE_INSTALL_LIBDIR@ NO_DEFAULT_PATH)
find_library(CERNLIB_PHTOOLS        NAMES phtools         HINTS @PACKAGE_CMAKE_INSTALL_LIBDIR@ NO_DEFAULT_PATH)

set(CERNLIB_LIBRARIES )
if(EXISTS ${CERNLIB_PAWLIB})
  list( APPEND  CERNLIB_LIBRARIES ${CERNLIB_PAWLIB})
endif()
if(EXISTS ${CERNLIB_PAWLIBLESSTIF})
  list( APPEND  CERNLIB_LIBRARIES ${CERNLIB_PAWLIBLESSTIF})
endif()

if(EXISTS ${CERNLIB_KERNLIB})
  list( APPEND  CERNLIB_LIBRARIES ${CERNLIB_KERNLIB})
endif()

if(EXISTS ${CERNLIB_PACKLIB})
  list( APPEND  CERNLIB_LIBRARIES ${CERNLIB_PACKLIB})
endif()

if(EXISTS ${CERNLIB_PACKLIBLESSTIF})
  list( APPEND  CERNLIB_LIBRARIES ${CERNLIB_PACKLIBLESSTIF})
endif()

if(EXISTS ${CERNLIB_GRAFLIB})
  list( APPEND  CERNLIB_LIBRARIES ${CERNLIB_GRAFLIB})
endif()

if(EXISTS ${CERNLIB_GRAFX11})
  list( APPEND  CERNLIB_LIBRARIES ${CERNLIB_GRAFX11})
endif()

if(EXISTS ${CERNLIB_MATHLIB})
  list( APPEND  CERNLIB_LIBRARIES ${CERNLIB_MATHLIB})
endif()

if(EXISTS ${CERNLIB_PHTOOLS})
  list( APPEND  CERNLIB_LIBRARIES ${CERNLIB_PHTOOLS})
endif()


find_library(CERNLIB_PAWLIB_STATIC         NAMES libpawlib.a          HINTS @PACKAGE_CMAKE_INSTALL_LIBDIR@ NO_DEFAULT_PATH)
find_library(CERNLIB_PAWLIBLESSTIF_STATIC  NAMES libpawlib-lesstif.a  HINTS @PACKAGE_CMAKE_INSTALL_LIBDIR@ NO_DEFAULT_PATH)
find_library(CERNLIB_KERNLIB_STATIC        NAMES libkernlib.a         HINTS @PACKAGE_CMAKE_INSTALL_LIBDIR@ NO_DEFAULT_PATH)
find_library(CERNLIB_PACKLIB_STATIC        NAMES libpacklib.a         HINTS @PACKAGE_CMAKE_INSTALL_LIBDIR@ NO_DEFAULT_PATH)
find_library(CERNLIB_PACKLIBLESSTIF_STATIC NAMES libpacklib-lesstif.a HINTS @PACKAGE_CMAKE_INSTALL_LIBDIR@ NO_DEFAULT_PATH)
find_library(CERNLIB_GRAFLIB_STATIC        NAMES libgraflib.a         HINTS @PACKAGE_CMAKE_INSTALL_LIBDIR@ NO_DEFAULT_PATH)
find_library(CERNLIB_GRAFX11_STATIC        NAMES libgrafX11.a         HINTS @PACKAGE_CMAKE_INSTALL_LIBDIR@ NO_DEFAULT_PATH)
find_library(CERNLIB_MATHLIB_STATIC        NAMES libmathlib.a         HINTS @PACKAGE_CMAKE_INSTALL_LIBDIR@ NO_DEFAULT_PATH)
find_library(CERNLIB_PHTOOLS_STATIC        NAMES libphtools.a         HINTS @PACKAGE_CMAKE_INSTALL_LIBDIR@ NO_DEFAULT_PATH)

set(CERNLIB_LIBRARIES_STATIC )
if(EXISTS ${CERNLIB_PAWLIB_STATIC})
  list( APPEND  CERNLIB_LIBRARIES_STATIC ${CERNLIB_PAWLIB_STATIC})
endif()
if(EXISTS ${CERNLIB_PAWLIBLESSTIF_STATIC})
  list( APPEND  CERNLIB_LIBRARIES_STATIC ${CERNLIB_PAWLIBLESSTIF_STATIC})
endif()

if(EXISTS ${CERNLIB_KERNLIB_STATIC})
  list( APPEND  CERNLIB_LIBRARIES_STATIC ${CERNLIB_KERNLIB_STATIC})
endif()

if(EXISTS ${CERNLIB_PACKLIB_STATIC})
  list( APPEND  CERNLIB_LIBRARIES_STATIC ${CERNLIB_PACKLIB_STATIC})
endif()

if(EXISTS ${CERNLIB_PACKLIBLESSTIF_STATIC})
  list( APPEND  CERNLIB_LIBRARIES_STATIC ${CERNLIB_PACKLIBLESSTIF_STATIC})
endif()

if(EXISTS ${CERNLIB_GRAFLIB_STATIC})
  list( APPEND  CERNLIB_LIBRARIES_STATIC ${CERNLIB_GRAFLIB_STATIC})
endif()

if(EXISTS ${CERNLIB_GRAFX11_STATIC})
  list( APPEND  CERNLIB_LIBRARIES_STATIC ${CERNLIB_GRAFX11_STATIC})
endif()

if(EXISTS ${CERNLIB_MATHLIB_STATIC})
  list( APPEND  CERNLIB_LIBRARIES_STATIC ${CERNLIB_MATHLIB_STATIC})
endif()

if(EXISTS ${CERNLIB_PHTOOLS_STATIC})
  list( APPEND  CERNLIB_LIBRARIES_STATIC ${CERNLIB_PHTOOLS_STATIC})
endif()

