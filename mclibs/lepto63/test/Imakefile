
DefinePackageLibrary(NullParameter)

CernlibFortranProgramTarget(demo,demo.o,$(PACKAGE_LIB),$(PACKAGE_LIB),jetset74 pdflib804 mathlib packlib kernlib)

CernlibFortranProgramTarget(demo2,demo2.o,$(PACKAGE_LIB),$(PACKAGE_LIB),jetset74 pdflib804 mathlib packlib kernlib)

TestTarget(demo,NullParameter,NullParameter)
TestTarget(demo2,NullParameter,NullParameter)

